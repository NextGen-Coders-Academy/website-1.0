<%- include('../partials/head') %>
<h3>All Events:</h3>
<% events.forEach(function(event) { %>
    <p><%= event.name %></p>
    <a class="button" href="/events/<%= event.id %>">Go To Event!</a>
<% }) %>
<%- include('../partials/footer') %>